import { Meta } from "@storybook/react";
import { Pricing } from "./Pricing";

export default {
  title: "Pages/Pricing",
  component: Pricing,
  parameters: {
    layout: "fullscreen",
  },
} as Meta;

export const Default = {
  args: {},
};
